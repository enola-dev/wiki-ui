{
  "name": "wiki-ui",
  "version": "1.0.0",
  "description": "Wiki UI assets and client-side bundle",
  "module": "dist/wiki.js",
  "scripts": {
    "dev": "bun --hot demo/server.ts",
    "preview": "bun run build && bun demo/server.ts --preview",
    "build": "rm -rf dist && bun build src/mermaid.ts --outdir dist --target browser --format esm --minify && bun build src/code.ts --outdir dist --target browser --format esm --minify && bun build src/wiki.ts --outdir dist --target browser --format esm --external './mermaid.js' --external './mermaid' --external 'mermaid' --external './code.js' --external './code' --external 'code' --minify",
    "test": "bun test",
    "check": "bun x tsc --noEmit && bun test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "highlight.js": "^11.11.1",
    "mermaid": "^11.17.2"
  }
}
